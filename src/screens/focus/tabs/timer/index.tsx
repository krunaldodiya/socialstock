import React from "react";
import { SafeAreaView, View, Text } from "react-native";

export const Timer = () => {
  return (
    <SafeAreaView style={{ flex: 1 }}>
      <View style={{ flex: 1 }}>
        <Text>Timer</Text>
      </View>
    </SafeAreaView>
  );
};
